<UserControl x:Class="Echo.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Echo.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Echo.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BoolToHorizontalAlignmentConverter x:Key="BoolToHorizontalAlignmentConverter" />
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="{StaticResource MaterialDesign.Brush.Primary.Dark}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Command="{Binding OpenRecentChatListCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}">
                    <materialDesign:PackIcon Foreground="White" Kind="History" />
                </Button>
                <Button Grid.Column="1"
                        Command="{Binding OpenContactListViewCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}">
                    <materialDesign:PackIcon Foreground="White" Kind="People" />
                </Button>
                <Button Grid.Column="2" Style="{StaticResource MaterialDesignFlatButton}">
                    <materialDesign:PackIcon Foreground="White" Kind="Archive" />
                </Button>
            </Grid>
            <ContentControl prism:RegionManager.RegionName="ChatListViewRegion" />
        </StackPanel>
        <ContentControl Grid.Column="1" prism:RegionManager.RegionName="ChatContentViewRegion" />
    </Grid>
</UserControl>
